# agents/

Custom subagent definitions (see the Claude Code docs on subagents). Empty
by default; the installer symlinks this directory into `~/.claude` so any
agent you add here is versioned and follows you across machines.
