# commands/

Custom slash commands (see the Claude Code docs on custom commands). Empty
by default; the installer symlinks this directory into `~/.claude` so any
command you add here is versioned and follows you across machines.
