Find the thing below in your own docs (notes, memory files, session context;
the open web is `/research`, not this). Lightweight command, not a persona:
no turn report, no wrap-up ritual. What to find, if it was given: $ARGUMENTS

- Read-only: Glob/Grep/Read over the repos granted to this session and, on
  a machine, `session/context/` and `session/working/`
  (rules/claude/session-dirs.md). Never a shell one-liner for a file-contents or
  file-location question; Grep and Glob answer both for free
  (rules/claude/permission-loops.md).
- Say where you found it (file path, plus its direct web URL per
  rules/shared/style.md if it is somewhere that gets read from a phone) rather than
  just quoting content with no source.
- Nothing found is a valid, useful answer: say so plainly instead of
  guessing at what was meant.
