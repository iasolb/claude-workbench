Research the topic below on the open web (your own docs are `/find`, not
this). Lightweight command, not a persona: no turn report, no wrap-up
ritual. Topic, if one was given: $ARGUMENTS

- WebSearch/WebFetch only: this is read-only research, never a change to
  any repo. If the research is actually in service of a code change, say so
  and route it to the right persona (`/driver` for a card, `/quarterback`
  for a structural question) instead of editing files under this command.
- Cite what you found with real links, not a paraphrase with no source.
- Memory-integrity rule 2 still applies: a plausible-sounding web answer is
  not verified until you have actually read the source, not just a summary
  of it.
