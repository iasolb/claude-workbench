# rules/

Standing rules, one file per topic, each imported by `CLAUDE.md` with an
`@rules/<topic>.md` line. One-topic-per-file keeps rules easy to update and
easy for Claude to cite. Copy starters from `examples/rules/` and edit, or
write your own. This README is not imported; only files referenced from
`CLAUDE.md` are loaded.
